apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: mongo-jiva-client
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app : mongo-jiva-client
    spec:
      containers:
        - name: mongo-jiva-client-container
          image: openebs/www-mongo-jiva-client:ghgh89
          imagePullPolicy: Always
          ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: 
  name: mongo-jiva-client-service
spec:
  type: NodePort
  ports:
  - name: mongo-jiva-client
    port: 80
    protocol: TCP
  selector:
    app: mongo-jiva-client          
